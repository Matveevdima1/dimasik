<Window x:Class="bd2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:bd2"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="800">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="Title">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13pt"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="15"/>
        </Style>
    </Window.Resources>
    <Grid Width="800" Height="350">
        <Grid.RowDefinitions>
            <RowDefinition Height="100*">
            </RowDefinition>
            <RowDefinition Height="350*">

            </RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,272" Grid.RowSpan="2">
            <Image Source="C:\Users\ДимаВащеКласс\source\repos\bd2\bd2\Resources\spike.jpg" Width="60"/>
            <TextBlock Text="WorldSkills" TextAlignment="Center" Style="{StaticResource Title}"/>


        </StackPanel>
        <Frame Height="250" Name="FrmMain" NavigationUIVisibility="Hidden" Margin="0,68,0,10" Grid.RowSpan="2"/>
    </Grid>
</Window>
